OpenARDP repository inventory
Generated for feature 002 convergence on 2026-07-22.

Scope
-----
This inventory describes the 152 version-controlled files expected after the
domain-contract change is staged. Ignored environments, caches, build
artifacts, coverage output and operating-system metadata are excluded.

Area                          Files
----------------------------  -----
.agents                          10
.github                           1
.specify                         21
docs                             21
prompts                           1
repository root                 16
schemas                           6
scripts                           6
spec-kit                         21
specs                            21
src/openardp                     14
tests                            14
----------------------------  -----
Total                           152

Reproduce
---------
Run the following after staging the intended change:

    git ls-files | LC_ALL=C sort
    git ls-files | wc -l

Boundary notes
--------------
- `.agents/` contains only the ten reviewed Spec Kit `SKILL.md` files.
- `.specify/` contains the reviewed Spec Kit execution layer and active-feature
  locator; it contains no credentials or local agent sessions.
- `src/openardp/domain/` contains only the reviewed F002 pure record,
  validation and canonical-identity modules. Ports, adapters, services and
  interfaces remain empty.
- Parser, persistence, retrieval, context-compilation, provider and CLI
  behavior remain absent because later work packages own those contracts.
- The authoritative exact path list is Git itself; this summary is checked
  during convergence to avoid maintaining a second hand-written file manifest.
