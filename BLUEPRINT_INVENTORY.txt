OpenARDP repository inventory
Generated for feature 001 convergence on 2026-07-22.

Scope
-----
This inventory describes the 120 version-controlled files expected after the
repository-baseline change is staged. Ignored environments, caches, build
artifacts, coverage output and operating-system metadata are excluded.

Area                          Files
----------------------------  -----
.agents                          10
.github                           1
.specify                         21
docs                             20
prompts                           1
repository root                 16
schemas                           3
scripts                           5
spec-kit                         21
specs                            11
src/openardp                      7
tests                             4
----------------------------  -----
Total                           120

Reproduce
---------
Run the following after staging the intended change:

    git ls-files | LC_ALL=C sort
    git ls-files | wc -l

Boundary notes
--------------
- `.agents/` contains only the ten reviewed Spec Kit `SKILL.md` files.
- `.specify/` contains the reviewed Spec Kit execution layer and active-feature
  locator; it contains no credentials or local agent sessions.
- `src/openardp/` contains only package metadata, `py.typed` and the five empty
  architectural namespaces. Product behavior is intentionally absent.
- Deleted blueprint placeholders (`core.py`, domain models, CLI and their two
  behavior tests) are not part of the baseline because later work packages own
  those contracts.
- The authoritative exact path list is Git itself; this summary is checked
  during convergence to avoid maintaining a second hand-written file manifest.
