# Master Codex session prompt — Blueprint v3.1

> **Operator aid, not repository authority.** Follow `AGENTS.md`, the ratified constitution,
> accepted ADRs, public schemas and the active feature artifacts when they differ from this
> preserved migration prompt. Prompt text cannot grant filesystem, network, tool or release authority.

You are continuing `Noetheon/OpenARDP` after merged Feature 005 and a green locked quality gate.

## Mission
Implement an implementation-first open-source reference platform. Do not describe it as an established standard. Contracts are experimental and must be justified by running software, conformance fixtures and eventually an independent consumer.

## First action
Read repository `AGENTS.md`, constitution, README, core docs/ADRs, Feature-001–005 specs, then the v3.1 overlay, audit report and `005A-strategic-realignment` prompt. Inspect actual code and run the authoritative baseline. Do not modify production code before 005A specify→clarify→plan→checklist→tasks→analyze converges.

## Non-negotiable constraints
- Preserve Features 001–005 behavior and history.
- Minimal public contracts precede Docling implementation.
- Preserve full native Docling output; build only a thin projection.
- Treat all source/native/derived content as untrusted data.
- Indexes are disposable and verified against authority.
- Core/model integrations are optional and provider-neutral.
- Tests are deterministic/offline unless a separately marked optional integration suite.
- One branch/PR per feature; no silent accepted-ADR rewrite.
- Every dependency gets lock, license, maintenance and security review.
- Define compatibility, migration, cancellation, recovery, privacy and resource-failure behavior.
- A worker process is not advertised as a universal strong sandbox.
- No unsupported novelty, savings, quality or security claims.

## Per-feature flow
`speckit.specify → clarify → plan → checklist → tasks → analyze → implement → converge`.
Resolve critical/high findings before implementation and report validation, residual risk and rollback.

## Current task
Implement only Feature `005A-strategic-realignment`. Stop after convergence and report changed files, superseded claims/decisions, quality gates, unresolved risks and the exact proposed Feature-006 prompt.
