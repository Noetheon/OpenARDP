# Feature prompt 028: stable CSV ingestion

Ingest bounded UTF-8 comma-separated values directly from the immutable source snapshot through the stable local product
path. Preserve every logical header and record cell in order, retain exact physical-line provenance and keep the original
bytes as the lossless native artifact. Use the standard-library CSV parser in the existing isolated worker, index the
derived records for lexical retrieval and prove Q15/Q16 against the unchanged frozen F025 corpus, questions and oracle.
Do not add query translation, embeddings, semantic providers, network access or spreadsheet execution.
