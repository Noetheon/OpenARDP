# F029 input

Implement and evaluate optional provider-neutral semantic and multilingual retrieval against the byte-identical frozen
F025 corpus, questions, support atoms, source roles and scoring protocol. Preserve F027 as the default offline lexical
profile, ingest CSV through F028, add no cloud default, and retain unfavorable evidence.
