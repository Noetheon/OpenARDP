# F038 Research

The existing `_render_context` ignores an already opted-in `bundle`; the JSON path already carries it. Correct the human projection only. Rich provenance contains projection/reference identity, not an inline page locator, so the renderer must not fabricate page numbers. `get-evidence --json` exposes the authoritative anchor separately. The ordinary output must remain body-free.

Thirty repeated questions across three arms would confound learning in a single-user trial. Ten pre-matched triplets of distinct real tasks with frozen randomized arm assignment and separately balanced run order is a small practical alternative (n=10 per arm, no broad inference). A manual blinded human supporting-span review avoids treating answer-word presence as semantic support.

Legacy F025/F034 relevance uses substring variants and F034 source fitness is a fixed metadata rubric. H076 labels a title containing the answer term relevant even though it is not by itself a definition; another selected passage does support the actual task. Preserve all frozen artifacts and evaluators; disclose these limits and do not claim that every legacy completed task was false.

The adopted 30%, 9/10 and 7/10 thresholds are investment heuristics, not validated market/scientific standards. Their prospective nature and a valid pause outcome must be explicit. No new runtime benchmark software is needed.
