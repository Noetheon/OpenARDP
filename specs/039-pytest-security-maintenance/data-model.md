# Data-model impact

No domain, persisted or interchange data model changes. The only updated metadata is the development dependency requirement and its existing lock record. Public schema versions and workspace migrations remain unchanged.
